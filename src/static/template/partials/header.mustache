<!DOCTYPE html>
<html lang="en">
<head>
    <base href="{{BaseUrl}}" />
    <meta charset="utf-8">
    <title>{{Title}}</title>
    <meta name="description" content="{{Description}}">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    {{#Head.Styles}}
    <link href="{{Style}}" rel="stylesheet" type="text/css">
    {{/Head.Styles}}

    {{#Doc.Styles}}
        <link href="{{Style}}" rel="stylesheet" type="text/css">
    {{/Doc.Styles}}

    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
    <script src="/static/js/html5shiv.js"></script>
    <![endif]-->

    <link rel="shortcut icon" href="{{Head.Favicon}}">
</head>

<body data-spy="scroll" data-offset="10" data-target="#sidebar-nav">

<nav class="navbar navbar-inverse navbar-static-top " role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex7-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <span class="navbar-brand">{{Brand}} . {{Breadcrumb}}</span>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex7-collapse">
        <ul class="nav navbar-nav navbar-right">
            {{#Header.Navigation}}
                <li id="{{Id}}"><a href="{{Url}}">{{#Icon}}<i class="{{.}} icon-large"></i>{{/Icon}} {{Name}}</a></li>
            {{/Header.Navigation}}
        </ul>
    </div><!-- /.navbar-collapse -->
</nav>

<header class="jumbotron masthead">
    <div class="container">
        <p>&nbsp;</p>
        {{#Teaser.Title}}<h1>{{/Teaser.Title}}{{#Teaser.Logo}}<img src="{{.}}"/>{{/Teaser.Logo}}{{#Teaser.Title}} {{.}}</h1>{{/Teaser.Title}}
        {{#Teaser.Description}}<p class="lead">{{.}}</p>{{/Teaser.Description}}
        {{#Teaser.Download}}<a id="download-link" {{#Teaser.OnClick}}onclick="{{.}}" {{/Teaser.OnClick}}class="btn btn-primary btn-lg"><i class="icon-download icon-large"></i> {{.}}</a>{{/Teaser.Download}}
    </div>
</header>

<div class="container main">